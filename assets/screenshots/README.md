# Screenshots

The README references two images from this folder. Add real captures here before making the
repository public, using these exact filenames so the README links resolve:

- `popover.png` — the dashboard popover, showing provider-grouped meters with a few metrics.
- `menu-bar.png` — the menu bar strip with a couple of pinned metrics visible.

Tips:

- Capture on a Retina display and keep the popover at its natural width.
- Use real but unremarkable account data, or a demo account, so nothing sensitive is shown.
- PNG, trimmed to the relevant UI, is plenty; no need for huge full-screen shots.
